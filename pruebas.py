## probar y armar funcion de comb y perm ##
from scipy.special import comb, perm

perm (3,2) #Calcula el número de permutaciones 6
comb (3,2) #Calcula el número de combinaciones 3

import subprocess

def chmac():
    ### Buscar SI ES POSIBLE###
    ## DEFINIR CAMBIO DE MAC ##
    subprocess.run(set randomization enabled=yes interface="Wi-Fi")
    ### La interfaz no admite la selección aleatoria o estaba bloqueada por un administrador.

## si es compatible el usar mac aleatoria ##
#ntries=random.randint(5,20)

#lineas a probar
subprocess.run(["netsh","wlan","add","profile","filename=.\\temp\\"+ red +".xml"])
connection_statuss=str(subprocess.run(["netsh","wlan","connect","name="+red], capture_output=True))
interface=str(subprocess.run(["netsh","wlan","show","interface"], capture_output=True))


re.compile(r"\t\t\t\t<keyMaterial>([\w \d \s \- \.]*)</keyMaterial>\n")
    